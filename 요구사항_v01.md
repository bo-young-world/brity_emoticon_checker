# 이모티콘 검수 도구 — 요구사항 v01

- 대상 저장소: `brity_emoticon_checker`
- 기준 문서: [PRD.md](PRD.md) / [디자이너_규격_체크리스트.md](디자이너_규격_체크리스트.md)
- 작성 방식: 요청 5건 분석 → 10문항 인터뷰 → 확정된 답변을 반영해 본 문서(v01) 작성
- 개발 범위: **항목 1~4 구현**, 항목 5(폴더 구조 FAIL)는 **TBD — 2차 개발(v02)** 로 이월

## 0. 요청 배경 (원 요청 5건)

1. 대화방 이미지 > Desktop, Mobile 외곽선 삭제
2. `_detail_thumb.png` 배경색 수정 — 체크리스트에 배경색 있음/없음으로 표시, WARN 공지
3. 체크리스트 맨 처음에 전체 용량 추가 (KB 표시)
4. 파일명 공백을 `_` 로 수정
5. 폴더 구조 분석 후 FAIL 시 공지 (상세 규칙 미정 — TBD)

## 1. 인터뷰 질의응답 요약

| # | 질문 | 답변 |
|---|------|------|
| 1 | 대화방 이미지 외곽선 규정 삭제 시 검사도 삭제할지 | **검사 완전 삭제** |
| 2 | 외곽선 삭제 범위 — 대화방만 vs group 폴더 포함 | **전체(대화방 + group 4종) 삭제** |
| 3 | detail_thumb 배경색 수정의 의미 — 규격 자체 변경 vs 표시 기능만 추가 | **기존 배경색(#FFFFFF) 규정 삭제, 배경색 있음/없음 체크로 전환** |
| 4 | 배경 있음/없음 판정 기준 | **알파 채널(투명 여부)로 자동 판정** |
| 5 | 전체 용량 집계 범위 — 디바이스 폴더 단위 vs 상위 폴더 합산 | **상위 폴더(MOBILE+DESKTOP) 합산** |
| 6 | KB 표시 변경 범위 — 표시만 vs 기준치(2MB)도 변경 | **표시만 KB, 기준치 2MB 그대로 유지** |
| 7 | 파일명 공백 처리 — 경고만 vs 자동 리네임 기능 | **자동 리네임 버튼 추가** |
| 8 | 자동 리네임 대상 — 파일명만 vs 폴더명 포함 | **폴더명도 포함** |
| 9 | 폴더 구조 FAIL 규칙 — 이번 범위 포함 여부 | **이번 범위 제외, 2차 개발(v02)에서 진행** |
| 10 | 산출물 반영 방식 — main 직접 커밋 vs 브랜치+PR | **main에 바로 커밋/push** |

## 2. 요구사항 상세 (AS-IS → TO-BE)

### 2-1. 외곽선(색상) 검사 삭제

| 구분 | 내용 |
|---|---|
| AS-IS | `spec.py: OUTLINE_COLORS`(group 4종), `CHAT_OUTLINE_COLOR`(대화방)를 기준으로, `rules.py: _check_outline_color()` 가 경계 안쪽 1px 링 색을 측정해 WARN 판정 |
| TO-BE | 외곽선 관련 상수·검사 로직 전체 삭제. group 4종(tab_normal/hover, list_thumb, detail_thumb) + 대화방 이미지(large/small, Mobile/Desktop) 모두 대상 |
| 영향 파일 | `src/spec.py` (`OUTLINE_COLORS`, `CHAT_OUTLINE_COLOR`, `OUTLINE_TOLERANCE` 삭제), `src/rules.py` (`_hex_to_rgb`, `_outline_color`, `_check_outline_color` 삭제 및 호출부 제거) |
| 문서 반영 | PRD.md 각 규격 표에서 외곽선 색상/두께 컬럼 삭제, 디자이너_규격_체크리스트.md 동일 반영 완료 |

### 2-2. `_detail_thumb.png` 배경 있음/없음 자동 판정

| 구분 | 내용 |
|---|---|
| AS-IS | 배경색 고정 규정(Mobile: 투명, Desktop: #FFFFFF)만 문서에 존재, 코드상 detail_thumb 전용 배경 검사 없음 |
| TO-BE | Mobile/Desktop, large/small `_detail_thumb.png` 전체에 대해 알파 채널로 배경 유무를 측정해 **"배경색 있음" / "배경색 없음(투명)"** 을 WARN 으로 공지 (FAIL 아님, 눈검수 유도) |
| 판정 로직 | 이미지 알파 채널 최소값(`alpha_min`) == 255 → 완전 불투명 → "배경색 있음", 그 외(투명 픽셀 존재) → "배경색 없음" |
| 영향 파일 | `src/spec.py` (새 상수 `BACKGROUND_CHECK_FILES`), `src/rules.py` (새 함수 `_check_background_presence()`, `_check_group_files()` 내 호출 추가) |

### 2-3. 전체 용량 KB 표시 (맨 처음)

| 구분 | 내용 |
|---|---|
| AS-IS | 디바이스 폴더 단위로 `_check_junk_and_size()` 가 MB 단위로 표시, 결과 목록 후반부에 등장 |
| TO-BE | 선택한 상위 폴더 기준(MOBILE+DESKTOP 합산) 전체 용량을 **KB** 로 계산해 결과 화면/CLI 출력 **맨 처음**에 표시. 기존 디바이스 폴더별 2MB FAIL 판정은 그대로 유지(표시 단위만 변경 아님 — 이 항목은 신규 요약 정보) |
| 영향 파일 | `src/rules.py` (새 함수 `total_size_kb()`), `emoticon_checker.py` (`run_cli`, GUI `run_check`에서 결과 맨 앞에 표시) |

### 2-4. 파일명·폴더명 공백 자동 치환

| 구분 | 내용 |
|---|---|
| AS-IS | 공백은 기존 파일명 문자 규칙(`FILENAME_STEM_PATTERN`)에 걸려 FAIL로만 지적, 수동 수정 필요 |
| TO-BE | 공백이 있는 파일·폴더를 찾아 **[파일명 공백 수정]** 버튼 클릭 한 번으로 공백만 `_` 로 일괄 치환(파일명 + 폴더명 모두 대상). 하위 경로부터 처리해 상위 폴더 이름 변경 시 하위 파일 경로가 깨지지 않도록 함. 기존 FAIL 검사 로직은 유지(공백 있는 상태는 계속 FAIL로 표시, 버튼은 그 FAIL을 해소하는 수단) |
| 영향 파일 | `src/rules.py` (새 함수 `find_space_named()`, `rename_spaces_to_underscore()`), `emoticon_checker.py` (GUI에 `[파일명 공백 수정]` 버튼 추가, "불필요 파일 삭제" 버튼과 동일한 패턴) |

### 2-5. 폴더 구조 FAIL 검수 — TBD (v02)

| 구분 | 내용 |
|---|---|
| 현황 | 상세 규칙 미전달 — 사용자가 추후 폴더 구조를 자세히 설명 예정 |
| v01 범위 | 미구현. PRD.md / 체크리스트에 "TBD — 2차 개발 예정" 문구만 추가 |
| Next step | 상세 규칙 수신 → 요구사항_v02 작성 → `src/rules.py: _check_structure()` 보강 |

## 3. 영향 범위 요약 (코드)

| 파일 | 변경 내용 |
|---|---|
| `src/spec.py` | 외곽선 관련 상수 삭제, `BACKGROUND_CHECK_FILES` 추가 |
| `src/rules.py` | 외곽선 함수 삭제, 배경 있음/없음 판정 함수 추가, 전체 용량(KB) 집계 함수 추가, 공백 탐지·자동 리네임 함수 추가 |
| `emoticon_checker.py` | CLI: 맨 앞 KB 요약 출력 추가 / GUI: 결과 트리 맨 위 KB 요약 행 추가, `[파일명 공백 수정]` 버튼 추가 |
| `PRD.md` | 외곽선 컬럼 삭제, detail_thumb 배경 규정 변경, 용량/공백/폴더구조 TBD 문구 추가 (완료) |
| `디자이너_규격_체크리스트.md` | 동일 반영 + 섹션 순서 조정(용량을 맨 앞으로) (완료) |
| `readme.md` | 검사 항목 표 정합성 업데이트 (완료) |

## 4. 판정 레벨 정리 (v01 반영 후)

| 레벨 | 항목 |
|---|---|
| FAIL | 구조(필수 폴더), 파일명(소문자/허용문자/공백), 필수 파일, 이미지 크기, GIF 무한반복, 디바이스 폴더 용량 초과(2MB), 정보 글자수/이모지 |
| WARN | 여백, **배경 있음/없음(detail_thumb, 신규)**, 투명배경(탭), 잔재 파일 |
| 정보 | **전체 용량 합계(KB, 신규, 판정 없음 — 참고용)** |
| 삭제됨 | ~~외곽선 색 (WARN)~~ — v01 에서 검사 자체 삭제 |

## 5. 다음 단계

1. 본 문서(v01) 기준으로 `src/spec.py`, `src/rules.py`, `emoticon_checker.py` 수정
2. 로컬 동작 확인(가능한 범위 — GUI는 Windows 전용이라 이 환경에서는 정적 검증/문법 확인 위주)
3. `main` 브랜치에 직접 commit & push
4. 폴더 구조 상세 규칙 수신 시 `요구사항_v02.md` 작성 → 2차 개발 진행
