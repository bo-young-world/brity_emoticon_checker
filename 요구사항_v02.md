# 이모티콘 검수 도구 — 요구사항 v02

- 대상 저장소: `brity_emoticon_checker`
- 이어지는 문서: [요구사항_v01.md](요구사항_v01.md) — v01 항목 5(폴더 구조 FAIL)의 TBD를 해소
- 기준 문서: [PRD.md](PRD.md) / [디자이너_규격_체크리스트.md](디자이너_규격_체크리스트.md)

## 1. 배경

v01에서 "5. 폴더 구조 분석 후 FAIL 시 공지"는 상세 규칙 미정으로 TBD 처리했다.
사용자가 Mobile `group/large`, `group/small` 폴더 구조(파일명·사이즈)를 제시했는데,
대조 결과 **해당 사이즈 값은 이미 `src/spec.py: GROUP_FILES`에 동일하게 구현되어 있어
변경이 필요 없었다.** 따라서 실제로 새로 필요한 것은 "구조가 틀렸을 때 FAIL 내는 규칙"
이며, 후속 확인을 통해 아래 2가지로 범위를 확정했다.

## 2. 확정된 요구사항

### 2-1. 대화방(large/small) 최상위 폴더 필수화

| 구분 | 내용 |
|---|---|
| AS-IS | `_check_structure()`: `group/large`, `group/small`만 필수(FAIL), 대화방 최상위 `large`/`small` 폴더는 있으면 PASS 표시만 하고 없어도 무반응(선택) |
| TO-BE | `group/large`, `group/small`, `large`, `small` **4개 폴더 모두 필수** — 없으면 FAIL |
| 유지되는 부분 | 폴더가 있다는 전제하에 **그 안의 이미지 파일 자체는 여전히 선택** — `_check_chat_files()`의 "대화방 이미지 없음(선택 사항 — 통과)" 로직은 변경하지 않음 |

### 2-2. `group` 폴더 하위 뎁스(depth) 오류 감지

| 구분 | 내용 |
|---|---|
| AS-IS | 뎁스 오류 감지 없음 — `group` 바로 밑에 파일을 둬도, `group/large` 안에 폴더를 만들어도 지적하지 않음 |
| TO-BE | 새 함수 `_check_group_depth()` 추가, 아래 3가지를 FAIL 로 공지 |
| 규칙 1 | `group` 폴더 바로 밑에 파일이 있으면 FAIL — `group/large` 또는 `group/small` 안에 있어야 함 |
| 규칙 2 | `group` 폴더 밑에 `large`/`small` 이 아닌 하위 폴더가 있으면 FAIL |
| 규칙 3 | `group/large`, `group/small` 안에 또 하위 폴더가 있으면 FAIL (이미지 파일만 있어야 함) |

### 범위에서 제외한 것 (인터뷰에서 확인)

- 규격에 없는 추가 폴더(WARN)를 FAIL로 격상하는 것은 **이번 범위 아님** — 그대로 WARN 유지
- 대화방 최상위 폴더 밑(large/small 안)의 뎁스 오류 감지는 이번엔 요청되지 않아 미구현
  (group 폴더만 대상). 필요 시 동일한 패턴으로 추가 가능

## 3. 구현

| 파일 | 변경 내용 |
|---|---|
| `src/rules.py` | `_check_structure()`: 대화방 large/small 을 필수(FAIL) 목록에 포함. 신규 `_check_group_depth()`: group 바로 밑 파일/이상 폴더 FAIL, group/large·small 안의 하위 폴더 FAIL. `check_device_folder()` 흐름 안에서 `_check_structure()`가 자동으로 `_check_group_depth()`도 호출 |
| `PRD.md` | TBD 문구를 확정 규칙으로 교체, 개정 이력에 v02 추가 |
| `디자이너_규격_체크리스트.md` | "0. 폴더 구조" 섹션에 v02 규칙 추가 |
| `readme.md` | 검사 항목 표의 "구조" 행 업데이트 |

## 4. 검증 (합성 테스트 폴더로 CLI 확인)

- **Case A** (group만 있고 대화방 large/small 폴더 자체가 없음) →
  `[FAIL] 구조 | large | 필수 폴더 없음`, `[FAIL] 구조 | small | 필수 폴더 없음` 정상 발생
- **Case B** (`group/caseb_stray.png` 파일이 group 바로 밑에 있음 + `group/large/nested/` 폴더 존재) →
  `[FAIL] 구조 | group/caseb_stray.png | group 폴더 바로 밑에 파일이 있음 ...`,
  `[FAIL] 구조 | group/large/nested | group/large 안에 폴더가 있음(깊이 오류) ...` 정상 발생
- **회귀 확인**: 기존 정상 구조(대화방 폴더 존재, 뎁스 문제 없음) 폴더는 새 FAIL 없이 그대로 통과

## 5. 다음 단계

1. `main` 브랜치에 직접 commit & push (v01과 동일한 방식)
2. 필요 시 Desktop 쪽 폴더 구조도 동일 규칙 적용 여부 확인 (현재 로직은 device 구분 없이 공통 적용됨 — 별도 확인 불필요)
