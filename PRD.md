# 이모티콘 검수 자동화 프로그램 (PRD)

## 전체 업무 순서 (신규 이모티콘 추가)

1. 지라 이슈(메일로 받음) 등록 확인
2. **디자인 검수 및 수정사항 가이드** ← ★ 이 단계를 자동화
3. 고객 확인 완료되면 스테이지 반영
4. 운영 반영 및 디자인 검수
5. 최종 운영 완료 안내 메일 발송 (이슈 Closed 처리)

## 검수 보완사항

### 파일명 규칙

- 제안서 파일명과 이미지 파일명을 동일하게 수정
- 파일명·폴더명은 **모두 소문자**
- 파일명에 쓸 수 있는 문자: **영문자, 숫자, 언더바(_)** 만 (그 외 특수문자 불가)
- 제안서의 영문 이름(표시명)은 대문자·특수문자 적용 가능 — 단 파일명과는 별개
- v01: 파일명·폴더명에 **공백**이 있으면 FAIL로 지적하는 것은 기존과 동일하되,
  검수 도구에 **[파일명 공백 수정]** 버튼을 추가해 공백만 자동으로 언더바(`_`)로
  일괄 치환할 수 있다 (파일명·폴더명 모두 대상). 공백 외 다른 불허 문자는 자동 수정 대상이 아니며
  여전히 수동으로 고쳐야 한다.

#### 이모티콘명(표시명) 특수문자 기준 (문의 답변 정리)

> 티켓 2607079154926086 — 국문명 "크크크크 크알라 : 리부트" / 영문명 "KKKK Keuala : Reboot"

- 이모티콘 **표시명(국문/영문)**: 콜론(:) 같은 일반 텍스트 특수문자 **가능**,
  이모지(✅ 등) **불가**
- **파일명**: 특수문자 포함 불가 — 영어 대소문자·숫자·언더바만 가능
  (파일명 규칙상 실제로는 소문자만 사용)

#### 제안서 내 이미지 파일명 예시

- 타입: **이모티콘**(26×26 & 84×84, png) / **스티콘**(84×84, png) / **애니콘**(84×84, gif)
- 제안서의 스티콘 파일명 예시 (`번호_이름` 형식, 대문자·언더바 사용):

```
01_OMGing            09_Dont_Know         17_OhOh
02_Good_Morning      10_Not_Sure          18_ME
03_Not_Good_Morning  11_No_Thankyou       19_Yessir
04_Uh                12_Lets_Eat          20_Shock
05_Sorry             13_Hey_gogo          21_Overload
06_Hing              14_Round_House       22_Work_Commitment
07_Check_Please      15_First_Retreat     23_After_Million_Years
08_Okay              16_Alright           24_Work_Fail
```


### 이모티콘 정보

| 항목 | 기준 |
|------|------|
| 이모티콘 국문명 | 20자 이내 |
| 이모티콘 영문명 | 20자 이내, **파일명·폴더명과 동일하게** |
| 이모티콘 국문 설명 | 120자 이내 |
| 이모티콘 영문 설명 | 120자 이내 |

### 이모티콘 폴더·파일

- 폴더: 폴더 구조에 맞게 수정, 제작 가이드에 맞게 폴더명 수정
- 파일: 제작 가이드에 맞게 파일명 수정, **파일명은 모두 소문자**

### 기타

- 이모티콘 전체 파일 용량 **2MB 이내** (디바이스 폴더 하나 기준)
  - v01: 검수 시작 시 화면·리포트 맨 처음에 **전체 용량 합계를 KB 단위**로 표시
    (선택한 상위 폴더 기준 — MOBILE + DESKTOP 합산). 2MB 초과 시 FAIL 기준은 유지.
- 이모티콘 폴더 내 불필요한 파일 삭제
  (맥OS에서 작업하는 경우 압축 시 이모티콘 파일 외 불필요한 파일이 생기므로 삭제)

## 폴더 전달 형태 (디바이스별)

디바이스별로 아래 이름의 최상위 폴더로 전달받는다 (영문명이 `happy` 인 경우):

```
[MOBILE] happy/          [DESKTOP] happy/
├── group/
│   ├── large/           ← 탭/썸네일 PNG 4종
│   └── small/
├── large/               ← 대화방 GIF (happy_01.gif …)
└── small/
```

> **TBD (2차 개발 예정)**: 상위 폴더/디바이스 폴더 구조 자체(폴더 누락·잘못된 위치 등)를
> 정밀 분석해 FAIL로 공지하는 규칙은 아직 상세 기준이 확정되지 않았다. 상세 규칙 전달 후
> `요구사항_v02` 로 별도 반영 예정.

## 제작 규격 — Mobile (모바일)

### group 폴더 (PNG)
- 폴더
  - {이모티콘과 동일한 영문명 폴더} > group > large
  - {이모티콘과 동일한 영문명 폴더} > group > small

파일명 예시는 영문명이 `happy` 인 경우.

> v01: **외곽선(색상/두께) 규정 삭제** — 모든 파일에서 자동/눈검수 대상에서 제외.
> `happy_detail_thumb.png` 의 배경색은 고정 규정(#FFFFFF) 대신, 실제 배경
> **있음/없음**만 자동 판정(알파 채널 기준)해 WARN 으로 공지한다 — FAIL 아님.

| 상위폴더 | 폴더 | 파일명 (용도) | 크기 | 배경색 | 여백 | 가이드 |
|------|------|--------------|------|--------|------|--------|
| group | large | `happy_tab_normal.png` (탭 노멀) | 120×120px | 투명 | 상하좌우 각 4px | 6P |
| | | `happy_tab_hover.png` (탭 호버) | 120×120px | | | 6P |
| | | `happy_list_thumb.png` (추가 리스트 썸네일) | 120×120px | | | 7P |
| | | `happy_detail_thumb.png` (정보 디테일 썸네일) | 200×200px | **있음/없음 자동 판정**(WARN) | | 8P |
| | small | `happy_tab_normal.png` (탭 노멀) | 60×60px | 투명 | 상하좌우 각 4px | 6P |
| | | `happy_tab_hover.png` (탭 호버) | 60×60px | | | 6P |
| | | `happy_list_thumb.png` (추가 리스트 썸네일) | 60×60px | | | 7P |
| | | `happy_detail_thumb.png` (정보 디테일 썸네일) | 72×72px | **있음/없음 자동 판정**(WARN) | 200×200px 작업 이미지 축소 | 8P |

### 대화방 이미지 (GIF 또는 PNG)
- 폴더 : 
  - {이모티콘과 동일한 영문명 폴더} > large
  - {이모티콘과 동일한 영문명 폴더} > small

| 폴더 | 파일명 | 크기 | 애니메이션 반복(gif) | 가이드 |
|------|--------|------|---------------------|--------|
| large | `happy_01.gif`, `happy_02.gif`, `happy_03.gif` … | 200×200px | 4회 재생 후 멈춤 | 9P |
| small | `happy_01.gif`, `happy_02.gif`, `happy_03.gif` … | 72×72px | 4회 재생 후 멈춤 | 9P |

- 파일명(예시): `이모티콘 영문명(소문자)_숫자(두자리)` — 예) `happy_01.gif`
  ※ 숫자 위치 등 형식은 정렬용일 뿐 자유 — **제안서 파일명과 동일**하고
  문자 규칙(소문자/숫자/언더바)만 지키면 됨
- v01: **외곽선 규정(색상/두께) 삭제** — 대화방 이미지는 외곽선 검사 대상에서 제외
- TIP) 포토샵 타임라인에서 반복 재생 횟수 5회로 설정하면 실제 4회로 저장됨 (포토샵 버전에 따라 다를 수 있음)

## 제작 규격 — Desktop (데스크탑)

### group 폴더 (PNG)
- 폴더
  - {이모티콘과 동일한 영문명 폴더} > group > large
  - {이모티콘과 동일한 영문명 폴더} > group > small


> v01: **외곽선(색상/두께) 규정 삭제** — 모든 파일에서 자동/눈검수 대상에서 제외.
> `happy_detail_thumb.png` 의 배경색은 고정 규정(#FFFFFF) 대신, 실제 배경
> **있음/없음**만 자동 판정(알파 채널 기준)해 WARN 으로 공지한다 — FAIL 아님.

| 상위폴더 | 폴더 | 파일명 (용도) | 크기 | 배경색 | 여백 | 가이드 |
|------|------|--------------|------|--------|------|--------|
| group | large | `happy_tab_normal.png` (탭 노멀) | 40×34px | #E5E5E5 | 상하좌우 각 3px | 11P |
| | large | `happy_tab_hover.png` (탭 호버) | 40×34px | #FFFFFF | 상하좌우 각 3px | 12P |
| | large | `happy_list_thumb.png` (추가 리스트 썸네일) | 35×35px | #FFFFFF | 상하좌우 각 5px | 13P |
| | large | `happy_detail_thumb.png` (정보 디테일 썸네일) | 50×50px | **있음/없음 자동 판정**(WARN) | 상하좌우 각 6px | 14P |
| | small | 4종 모두 **large 폴더 이미지와 동일** (`happy_tab_normal.png` 40×34, `happy_tab_hover.png` 40×34, `happy_list_thumb.png` 35×35, `happy_detail_thumb.png` 50×50) | | | | 11~14P |

### 대화방 이미지 (GIF 또는 PNG)
- 폴더 : 
  - {이모티콘과 동일한 영문명 폴더} > large
  - {이모티콘과 동일한 영문명 폴더} > small

| 폴더 | 파일명 | 크기 | 애니메이션 반복(gif) | 가이드 |
|------|--------|------|---------------------|--------|
| large | `happy_01.gif`, `happy_02.gif`, `happy_03.gif` … | 84×84px | 4회 재생 후 멈춤 | 15P |
| small | `happy_01.gif`, `happy_02.gif`, `happy_03.gif` … | 26×26px | 4회 재생 후 멈춤 | 15P |

- 파일명(예시): `이모티콘 영문명(소문자)_숫자(두자리)` — 예) `happy_01.gif`
  ※ 숫자 위치 등 형식은 정렬용일 뿐 자유 — **제안서 파일명과 동일**하고
  문자 규칙(소문자/숫자/언더바)만 지키면 됨
- v01: **외곽선 규정(색상/두께) 삭제** — 대화방 이미지는 외곽선 검사 대상에서 제외
- TIP) 포토샵 타임라인에서 반복 재생 횟수 5회로 설정하면 실제 4회로 저장됨 (포토샵 버전에 따라 다를 수 있음)

---

※ 표에서 빈칸은 원본 문서(엑셀)에서 별도 명시가 없던 항목입니다.

## 개정 이력

- **v01** (요구사항_v01 반영):
  1. 대화방 이미지(Mobile/Desktop) 외곽선 규정 삭제
  2. group 폴더 4종(tab_normal/hover, list_thumb, detail_thumb) 외곽선 규정 삭제
  3. `_detail_thumb.png` 배경색 고정 규정(#FFFFFF) 삭제 → 배경 있음/없음 자동 판정(WARN)으로 대체
  4. 전체 용량을 검수 맨 처음에 KB 단위로 합산 표시 (2MB FAIL 기준은 유지)
  5. 파일명·폴더명 공백 → 언더바 자동 치환 기능 추가
  6. 폴더 구조 FAIL 검수 규칙은 TBD (2차 개발 예정)
