# 이모티콘 검수 도구 — 요구사항 v03

- 대상 저장소: `brity_emoticon_checker`
- 이어지는 문서: [요구사항_v01.md](요구사항_v01.md), [요구사항_v02.md](요구사항_v02.md)
- 기준 문서: [PRD.md](PRD.md) / [디자이너_규격_체크리스트.md](디자이너_규격_체크리스트.md)

## 1. 배경

v02에서 대화방(`large`/`small`) **폴더**를 필수로 바꾸면서도, 폴더 "안의 이미지 파일"은
기존 PRD 규정("대화방 이미지는 없어도 됨 — 선택 사항")을 그대로 유지했다. 이번에 사용자가
"폴더 안에 파일이 하나도 없어도 되는 게 맞나?"라고 확인 요청 → **선택 사항 규정 자체를
폐지**하고 최소 개수를 강제하는 것으로 확정했다.

## 2. 확정된 요구사항

### 2-1. 대화방 이미지 최소 개수 필수화

| 구분 | 내용 |
|---|---|
| AS-IS | `large`/`small` 폴더는 필수(v02)이나, 안의 파일 개수는 0개도 허용(PASS "선택 사항 — 통과") |
| TO-BE | `large`, `small` **폴더 각각 최소 10개 파일 필수**. 미만이면 FAIL (`대화방 이미지 N개 — 최소 10개 필요`) |
| 근거 상수 | `src/spec.py: CHAT_FILES_MIN = 10` |

### 2-2. 대화방 파일명 형식 고정

| 구분 | 내용 |
|---|---|
| AS-IS | 문자 규칙(소문자·숫자·언더바)만 검사, 이름 형식(숫자 위치 등)은 자유("제안서 파일명과 동일하면 됨") |
| TO-BE | **`{이모티콘 영문명(소문자)}_{숫자}.png` 또는 `.gif`** 형식만 허용. 그 외 형식(예: 접두어가 영문명과 다름, 숫자 없음)은 FAIL |
| 판정 메시지 | `파일명 형식 오류 — '{영문명}_숫자.png|gif' 형식이어야 함 (예: {영문명}_01.png)` |

## 3. 구현

| 파일 | 변경 내용 |
|---|---|
| `src/spec.py` | `CHAT_FILES_MIN = 10` 추가 |
| `src/rules.py` | `_check_chat_files()`: 파일명 검사를 일반 문자 규칙(`_STEM_RE`)에서 `^{en_name}_\d+$` 정규식으로 교체, 폴더별 파일 수가 `CHAT_FILES_MIN` 미만이면 FAIL 추가, 기존 "선택 사항 — 통과" PASS 메시지 제거 |
| `PRD.md` / `디자이너_규격_체크리스트.md` / `readme.md` | "대화방 이미지는 없어도 됨(선택)" 관련 문구를 전부 확정 규칙으로 교체 |

## 4. 검증 (합성 테스트 폴더로 CLI 확인)

- **5개만 있는 폴더** → `[FAIL] 파일 | large | 대화방 이미지 5개 — 최소 10개 필요` (small도 동일) 발생
- **정확히 10개, 올바른 이름(`{영문명}_숫자.png`)** → FAIL 0건, PASS 처리
- **10개지만 이름이 영문명과 다름(`wrongname_01.png` 등)** → 파일마다 `파일명 형식 오류` FAIL +
  유효 파일 0개로 집계되어 `최소 10개 필요` FAIL 추가 발생 (의도대로 이중 방지)

## 5. 다음 단계

1. `main` 브랜치에 직접 commit & push
2. Desktop 쪽도 동일 로직 공용 적용 확인 완료 (device 구분 없이 `_check_chat_files()` 공통 사용)
